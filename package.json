{
  "name": "ai-crm-monorepo",
  "version": "1.0.0",
  "description": "AI CRM Application - Deploys backend or frontend based on SERVICE_TYPE env var",
  "scripts": {
    "build": "BUILD_PHASE=build node deploy-service.js",
    "start": "BUILD_PHASE=start node deploy-service.js",
    "start:simple": "node simple-server.js",
    "build:backend": "cd backend && npm ci && npm run build",
    "start:backend": "cd backend && npm start",
    "build:frontend": "cd frontend && npm ci && npm run build", 
    "start:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": "20.x",
    "npm": "9.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}